import * as React from 'react';

const Reply: React.SFC<any> = () => (
    <div className='mail-reply'>
        <hr />
        <textarea className='form-control' rows={10}></textarea>
    </div>
);

export default Reply;
